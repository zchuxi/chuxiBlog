# 兜底机械检查点（命令与 scripts/git-hooks/pre-commit 保持一致，约定见 README「改动后验证」）：
# frontend/ 改动推送或 PR 时强制运行 npm run lint 与 npm run build，不依赖本机手动安装钩子
name: frontend-ci

on:
  push:
    branches: [master, main]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'
  pull_request:
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'

jobs:
  lint-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: npm ci
        run: npm ci
      - name: npm run lint
        run: npm run lint
      - name: npm run build
        run: npm run build
